Icons for Aikuma
----------------

This directory contains an icons.pxm (Pixelmator) file with vector graphics/fonts used for the icons.
Slices are defined on the image of the fonts, which means we can change the resolution and export the slices easily when we want different resolutions.