TODO
- Make a list view out of the timeline (so to automatically display it).


Saved State, Bundle Keys:

Users
	Array: users

User
	Image: "user_<uuid>.png"
	Name: users/<uuid>/name => "Some Name"

Current User
	id: users/current => <user uuid>

Original:
    Recording: original_<hash>
    User reference: original_<hash>_creator => "<user uuid>"