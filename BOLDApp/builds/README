Builds
======

This is the location to put tagged builds.

These builds are for convenience only, when somebody can't build the BOLD app on the road, for example.

Installation
------------

Run:
adb install -r BOLDApp-debug.apk